const sideBar = {
  SHOW_HIDE_SIDE_BAR: "SHOW_HIDE_SIDE_BAR"
};

const modalError = {
  SHOW_CLOSE_MODAL_ERROR: "SHOW_CLOSE_MODAL_ERROR",
  SET_DES: "SET_DES"
};

const modalLoading = {
  SHOW_CLOSE_MODAL_LOADING: "SHOW_CLOSE_MODAL_LOADING"
};

export const actionConstant = {
  sideBar,
  modalError,
  modalLoading
};
