export const config = {
    // API
    // API_RESOURCE_URL: "http://localhost:8000",
    API_RESOURCE_URL: "http://armsis.arms-asia.com:8080/api",
  
    // APP SETTINGS
    NETWORK_TIMEOUT: 10000, // milliseconds
  
    // DEV SETTING
    REDUX_LOG_ENABLED: true,
    REQUEST_LOG_ENABLED: false,
    RESPONSE_LOG_ENABLED: true
  };
